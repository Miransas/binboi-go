# internal/transport

Transport normalization and protocol inference live here.

This package turns user-facing target strings into validated internal transport metadata. It intentionally handles only a small, explicit set of protocols for now so the engine's current capabilities remain easy to reason about.
