# internal/tunnel

Tunnel-facing descriptors and planning scaffolding live here.

At this stage the package focuses on producing stable public tunnel metadata rather than implementing a full transport stack. That keeps the current repository honest while still giving the rest of the engine a real integration point.
