# internal/tunnel

Tunnel-facing descriptors, random subdomain allocation helpers, and durable tunnel records live here.

At this stage the package focuses on producing stable public tunnel metadata and tunnel routing state rather than implementing a full transport stack. That keeps the current repository honest while still giving the rest of the engine a real integration point.
