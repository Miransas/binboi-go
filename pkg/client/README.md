# pkg/client

This package is the small public client surface for the daemon control API and stream protocol.

It is useful for examples, automation hooks, tests, `binboi` itself, and future integrations from the private Binboi product repository.
