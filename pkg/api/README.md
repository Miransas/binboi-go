# pkg/api

This package contains public wire types for the daemon's control API.

Keeping these types small and explicit makes it easier for external clients, examples, and the private Binboi product repo to share a stable contract.
