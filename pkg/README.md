# pkg

This folder contains packages intended for external consumers.

- `pkg/api` defines the wire-level request and response types
- `pkg/client` provides a minimal client for talking to the daemon control API
