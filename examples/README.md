# examples

Examples in this folder demonstrate intended usage without pretending to be the full Binboi product.

- `http-basic` provides a tiny local upstream service
- `docker-basic` shows how the daemon can be composed into a containerized workflow
